//import { PUBLIC_API_URL } from "$env/static/public";
//import { MedicalActResultApi } from "$lib/api"

export const sendMedicalResult = (file: string, medicalActId: number) => { // file as base64
    // const medicalActResultApi = new MedicalActResultApi({basePath: PUBLIC_API_URL, accessToken: "test"});
    // medicalActResultApi.medicalActIdResultsPost({
    //     medical_act_id: medicalActId,
    //     file_name: "",
    //     file_data: file
    // }, medicalActId);
}